spring:
  application:
    name: stock-market-tracker

  datasource:
    url: jdbc:postgresql://localhost:5434/stock_market_tracker
    username: nahim
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: update
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8082